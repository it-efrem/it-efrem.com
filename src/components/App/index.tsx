import React from "react";
import styled from "styled-components";

const AppStyled = styled.div`
    color: red;
`;

export const App = () => {
    return (
        <AppStyled>App init</AppStyled>
    )
}
