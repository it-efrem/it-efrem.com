server {
        listen 80;

        location / {
            root      /app/packages/web_build;
            autoindex on;
        }

        location /update-your-browser {
            root      /app/packages/web_build/pages/update-your-browser;
            autoindex on;
        }
}